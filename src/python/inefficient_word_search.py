# inefficient word "search"
# version 0.2 (changed some variable names, commented out function call.)
# another tester script, word counter <= more accurate descriptor.
def thingy(a: list) -> int:
    return int(len(a))

def thingimajig():
    # gee, the locals sure are friendly
    silly_putty = str(input("Enter a string to count:  "))
    garbage = thingy(silly_putty)
    print(garbage)

#thingimajig()   <=== Uncomment as necessary.
